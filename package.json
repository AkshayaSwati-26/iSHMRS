{
  "name": "ishrms-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npm install && npx prisma db push",
    "start": "cd backend && npm start"
  }
}
